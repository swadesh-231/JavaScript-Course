let age = 22;        // value can be changed later
const name = "Rahul"; // value cannot be changed
var city = "Delhi";   // old way (avoid)

console.log(age,name,city);


const user = {
  name: "Rohit",
  age: 21,
  address: {
    city: "Kolkata",
    pin: 700001
  }
};

console.dir(user);
